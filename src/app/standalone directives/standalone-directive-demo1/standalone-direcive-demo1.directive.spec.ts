import { StandaloneDireciveDemo1Directive } from './standalone-direcive-demo1.directive';

describe('StandaloneDireciveDemo1Directive', () => {
  it('should create an instance', () => {
    const directive = new StandaloneDireciveDemo1Directive();
    expect(directive).toBeTruthy();
  });
});
