import { StandaloneDireciveDemo2Directive } from './standalone-direcive-demo2.directive';

describe('StandaloneDireciveDemo2Directive', () => {
  it('should create an instance', () => {
    const directive = new StandaloneDireciveDemo2Directive();
    expect(directive).toBeTruthy();
  });
});
