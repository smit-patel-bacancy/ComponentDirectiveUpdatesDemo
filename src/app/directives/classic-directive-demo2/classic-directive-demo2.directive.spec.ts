import { ClassicDirectiveDemo2Directive } from './classic-directive-demo2.directive';

describe('ClassicDirectiveDemo2Directive', () => {
  it('should create an instance', () => {
    const directive = new ClassicDirectiveDemo2Directive();
    expect(directive).toBeTruthy();
  });
});
