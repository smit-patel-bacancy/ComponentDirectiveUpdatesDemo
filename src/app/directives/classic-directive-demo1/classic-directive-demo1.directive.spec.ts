import { ClassicDirectiveDemo1Directive } from './classic-directive-demo1.directive';

describe('ClassicDirectiveDemo1Directive', () => {
  it('should create an instance', () => {
    const directive = new ClassicDirectiveDemo1Directive();
    expect(directive).toBeTruthy();
  });
});
